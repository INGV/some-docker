The files `gpd_predict.py` and `entrypoint.sh` must have execution permissions _before_ running Docker build

The PLOT option of gpd-predict, has been removed.
If you want to plot, create your personal stuff
